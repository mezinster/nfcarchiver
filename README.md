# Issue screenshots

An orphan branch that carries nothing but images referenced from GitHub issues.
It shares no history with `master` and is never merged — the GitHub CLI cannot
upload issue attachments, so images embedded in issues need a stable URL.
